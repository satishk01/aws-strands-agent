

### install git
sudo yum install git -y


### install uv and uvx
curl -LsSf https://astral.sh/uv/install.sh | sh

### set the virtual environment 

uv venv --python 3.12
source .venv/bin/activate

# Install Strands Agents
uv pip install strands-agents strands-agents-tools mcp pandas matplotlib

aws configure and set the region as we are using role based access for the rest.